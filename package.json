{
  "name": "desk-app-demo",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "homepage": ".",
  "dependencies": {
    "connected-react-router": "^6.3.2",
    "react": "^16.8.5",
    "react-dom": "^16.8.5",
    "react-redux": "^6.0.1",
    "react-router": "^5.0.0",
    "react-router-dom": "^5.0.0",
    "react-scripts": "2.1.8",
    "redux": "^4.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "packageDarwin": "electron-packager . app-os --platform=darwin --arch=x64 --out=./dist --asar --app-version=2.0.1",
    "packageWin": "electron-packager . app-win --platform=win32 --arch=x64 --out=./dist --asar --app-version=2.0.1",
    "packageLinux": "electron-packager . app --platform=linux --arch=x64 --out=./dist --asar --app-version=2.0.1"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "electron-packager": "^13.1.1",
    "extract-zip": "^1.6.7",
    "yargs-parser": "^13.0.0",
    "electron": "^4.1.1"
  }
}
